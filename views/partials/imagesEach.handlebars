<section class="card">
  <a href="/painting/{{id}}">
    <header>{{title}}</header>

    <div class="card-body">
      <img src="/images/{{filename}}" alt="{{description}}">
      <p>Created By: {{artist}}</p>
      <p>Date Created: {{timestamp}}</p>
    </div>
  </a>
</section>